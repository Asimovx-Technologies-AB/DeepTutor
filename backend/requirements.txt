# Deep Tutor MVP — Backend Dependencies
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9

# Database
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# RAG — Core
langchain>=0.2.0
langchain-community>=0.2.0
chromadb>=0.5.0
networkx>=3.3

# Document parsing — Primary (Docling) + Fallbacks
docling>=2.0.0            # IBM Docling: structure-aware PDF parsing, OCR, tables, formulas

# RAG — Advanced Retrieval (NEW)
rank-bm25>=0.2.2          # BM25+ reranking and hybrid search
cachetools>=5.3.0         # TTL-aware LRU caches for embedding + query results
scikit-learn>=1.4.0       # cosine similarity utilities

# Document processing (fallbacks)
pypdf2>=3.0.0
pypdf>=4.0.0
pdfplumber>=0.11.0
unstructured>=0.14.0

# HTTP / Async
httpx>=0.27.0
aiofiles>=23.2.1

# Utilities
python-dotenv>=1.0.0
tenacity>=8.3.0

# Model Context Protocol
mcp>=1.0.0
